# 📦 Prompt برای Import پروژه در اکانت جدید

**کپی کن و در اکانت جدید Manus paste کن** 👇

---

```
سلام! من یک پروژه AI Twitter Monitor دارم که می‌خوام به این اکانت منتقل کنم.

⚠️ **مهم:** فقط فایل‌هایی که بهت میدم رو استفاده کن. هیچ چیزی رو خودت نساز یا تغییر نده.

## 📋 مرحله ۱: ساخت پروژه خالی

یک پروژه جدید با این مشخصات بساز:

- **نام پروژه:** `ai-twitter-monitor-new`
- **Template:** `web-db-user` (React + tRPC + Database + Auth)
- **Features:** server, db, user

## 📂 مرحله ۲: آپلود فایل‌ها

الان 3 فایل بهت میدم:

1. **project-code.zip** (379 KB) - تمام کدهای پروژه
2. **database-backup-2025-11-11T16-47-47-877Z.json** (309 KB) - دیتابیس
3. **settings-template.json** - تنظیمات (باید API keys رو خودم پر کنم)

**مراحل:**

1. فایل `project-code.zip` رو unzip کن و تمام فایل‌ها رو در root پروژه قرار بده
2. اگر فایلی با نام مشابه وجود داشت، replace کن
3. هیچ فایلی رو ویرایش نکن

## 🔧 مرحله ۳: نصب Dependencies

```bash
cd /path/to/project
pnpm install
```

## 🗄️ مرحله ۴: Setup دیتابیس

ابتدا schema رو push کن:

```bash
pnpm db:push
```

بعد دیتابیس رو import کن:

```bash
npx tsx import-database.mjs database-backup-2025-11-11T16-47-47-877Z.json
```

**چک کن که درست import شد:**

```sql
SELECT COUNT(*) FROM users;      -- باید 3 باشد
SELECT COUNT(*) FROM tweets;     -- باید 149 باشد
SELECT COUNT(*) FROM settings;   -- باید 1 باشد
```

## ⚙️ مرحله ۵: تنظیمات

فایل `settings-template.json` رو باز کن. من باید این فیلدها رو پر کنم:

- `apifyToken` - توکن Apify من
- `telegramBotToken` - توکن ربات تلگرام من
- `openRouterApiKey` - کلید API OpenRouter من

بقیه تنظیمات از قبل پر شده‌اند.

بعد از پر کردن، بهت میگم که این تنظیمات رو در Settings وارد کنی.

## 🚀 مرحله ۶: راه‌اندازی

سرور رو اجرا کن:

```bash
pnpm dev
```

باید بدون خطا اجرا بشه.

## 🔗 مرحله ۷: Setup Telegram Bot

1. به صفحه Settings برو
2. روی دکمه "🤖 Setup Telegram Mini App" کلیک کن
3. منتظر پیام موفقیت بمون

## ✅ مرحله ۸: تست

این موارد رو تست کن:

1. Dashboard باز میشه؟
2. توییت‌ها نمایش داده میشن؟
3. Fetch کار می‌کنه؟
4. Telegram bot پاسخ میده؟

اگر همه ✅ بود، بهم بگو که پروژه با موفقیت منتقل شد! 🎉

اگر خطا دادی، خطا رو کامل کپی کن و بفرست تا کمکت کنم.
```

---

## 📊 خلاصه فایل‌ها

| فایل | حجم | توضیحات |
|------|-----|---------|
| `project-code.zip` | 379 KB | تمام کدهای پروژه (بدون node_modules) |
| `database-backup-*.json` | 309 KB | 3 کاربر، 149 توییت، 1 تنظیمات |
| `settings-template.json` | 2 KB | تنظیمات (API keys باید پر شوند) |

---

## 🔐 API Keys که باید پر کنی

بعد از import، این API keys رو در `settings-template.json` پر کن:

1. **Apify Token** - از https://console.apify.com
2. **Telegram Bot Token** - از @BotFather
3. **OpenRouter API Key** - از https://openrouter.ai

---

## ⚠️ نکات مهم

1. **فایل‌ها رو دقیق کپی کن** - حتی یک فایل کم یا زیاد نباشه
2. **هیچ چیزی رو ویرایش نکن** - فقط کپی‌پیست کن
3. **API keys رو امن نگه دار** - بعد از import، فایل settings-template.json رو حذف کن
4. **دیتابیس رو چک کن** - تعداد رکوردها باید دقیقاً مطابقت داشته باشه

---

## 🆘 مشکلات رایج

### مشکل: `pnpm install` خطا میده

**راه‌حل:**
```bash
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### مشکل: `pnpm db:push` خطا میده

**راه‌حل:**
بررسی کن که `DATABASE_URL` در environment variables تنظیم شده باشه.

### مشکل: دیتابیس import نمیشه

**راه‌حل:**
```bash
# بررسی فرمت فایل
head -n 10 database-backup-*.json

# اجرای مجدد
npx tsx import-database.mjs database-backup-*.json
```

### مشکل: Telegram bot پاسخ نمیده

**راه‌حل:**
1. به Settings برو
2. "Setup Telegram Mini App" رو دوباره کلیک کن
3. Bot Token رو بررسی کن

---

## ✅ Checklist نهایی

- [ ] پروژه ساخته شد
- [ ] فایل‌ها آپلود شدن
- [ ] `pnpm install` اجرا شد
- [ ] `pnpm db:push` اجرا شد
- [ ] دیتابیس import شد
- [ ] تعداد رکوردها درسته
- [ ] API keys وارد شدن
- [ ] سرور اجرا میشه
- [ ] Dashboard باز میشه
- [ ] توییت‌ها نمایش داده میشن
- [ ] Telegram bot setup شد
- [ ] Telegram bot پاسخ میده

اگر همه ✅ بود، پروژه با موفقیت منتقل شده! 🎉

---

**آخرین بروزرسانی:** 11 نوامبر 2025  
**نسخه:** 1.0
